{"task":"Give every auto-mode site a .localhost origin over plain HTTP","goals":"Let a headless browser reach these sites at all — several refuse a custom local TLD outright","status":"Completed","recorded_at":"2026-09-09T08:10:41Z","parent_commit":"6f7c292f47b586b514d079793c81c2f0623030f8","repo":"ldev","pct":"100","tasks":[{"id":"localhost-origin","content":"Serve every auto-mode site at <name>.localhost over plain HTTP as well, so a headless browser that refuses a custom local TLD still has a working origin","status":"completed"},{"id":"localhost-dashboard","content":"Serve the dashboard at bare http://localhost too","status":"completed"}],"agent":"claude-code","changelog":[{"text":"Adds an `http://*.localhost` site block serving the same per-site roots over plain HTTP, with root derived from the Host label (`__SITES__/{labels.1}.__TLD__`) and the same php/static/dashboard-fallback handling.","kind":"info","priority":"low","state":"complete","explicit":false,"tags":["architecture"]},{"text":"Adds an `http://localhost` block serving the dashboard over plain HTTP.","kind":"info","priority":"low","state":"complete","explicit":false,"tags":["architecture"]},{"text":"Rationale comment: automated/preview browsers (Claude, Playwright, CI images) block subresources on custom local TLDs, so every site gains a headless-reachable origin.","kind":"info","priority":"low","state":"complete","explicit":false,"tags":["docs"]}],"code_quality":[{"text":"The three-way `@php` / `@static` / fallback structure and the dashboard block are copied from the existing HTTPS site blocks; they will drift as soon as one side gains a directive (headers, logging, try_files).","kind":"info","priority":"low","state":"needs_review","tags":["duplication","readability"]}],"vulnerabilities":[{"text":"`root * __SITES__/{labels.1}.__TLD__` interpolates a client-controlled Host label into a filesystem path. Caddy only matches `*.localhost` (exactly two labels) and Go's server rejects Hosts containing `/`, so traversal is not reachable today — but this is the one spot where untrusted input reaches a path, worth keeping in mind if the matcher is ever widened to `*.*.localhost` or a placeholder other than `{labels.1}`.","kind":"info","priority":"low","state":"needs_review","tags":["security"]},{"text":"`templates/Caddyfile.auto.tmpl:88` — the new `http://` blocks make Caddy answer on port 80 for *any* request carrying a `Host: <name>.localhost` or `Host: localhost` header, including from other machines on the LAN, and `file_server browse` in the `@static` handler then lists site directory contents. The HTTPS sites at least required trusting the local CA. Bind these blocks to loopback only (e.g. address `http://*.localhost:80` served on a `bind 127.0.0.1 ::1` listener, or add `bind 127.0.0.1 ::1` inside both new blocks) so remote hosts cannot reach site roots or the dashboard.","kind":"task","priority":"high","state":"complete","resolved_by":"ac418767ad3340cf3a79b6c992a276ef51f1c8f8","tags":["security"],"history":[{"at":"2026-09-09T08:12:38.006Z","type":"resolved","by":"claude:84a94ca3-be6f-45bb-bfed-90e7f8bab9ad","commit":"ac418767ad3340cf3a79b6c992a276ef51f1c8f8"}]}],"code_style_suggestions":[{"text":"OK.","kind":"info","priority":"low","state":"complete","explicit":false}],"reusability":[{"text":"The site body (encode/@php/@static/fallback) now exists twice; a Caddyfile snippet (`(site_body) { … }` + `import site_body`) would keep the HTTPS and HTTP variants in sync.","kind":"info","priority":"low","state":"needs_review","tags":["duplication"]}],"system_compatibility":[{"text":"`*.localhost` is resolved by Chrome/Firefox internally, but macOS `getaddrinfo` does not resolve arbitrary `*.localhost`, so `curl https://foo.localhost` and other non-browser clients will still fail — the fallback helps browsers only.","kind":"info","priority":"low","state":"needs_review","tags":["compatibility"]}],"review_run":{"state":"ran","at":"2026-09-09T08:10:40Z","attempts":1,"agent":"claude","mode":"light"}}

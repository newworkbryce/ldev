{"task":"Bind the .localhost blocks to loopback","goals":"Keep the plain-HTTP fallback reachable only from this machine","status":"Completed","recorded_at":"2026-09-09T08:12:30Z","parent_commit":"618cd40a9e9065feb5ed4831a28fe1ec9a1f048a","repo":"ldev","pct":"100","tasks":[{"id":"bind-loopback","content":"Bind the new .localhost HTTP blocks to 127.0.0.1 and ::1 so no LAN host can reach a site root through them","status":"completed"},{"id":"verify-bind","content":"Verified with caddy adapt: the .localhost server now listens on 127.0.0.1:80 and [::1]:80 rather than :80, and the rendered config still validates","status":"completed"},{"id":"https-exposure","content":"Noted that the pre-existing *.<TLD> HTTPS server still listens on :443 across every interface with file_server browse behind it — filed as a concern, since binding it to loopback would also stop testing from a phone on the same network","status":"completed"}],"agent":"claude-code","changelog":[{"text":"The .localhost HTTP blocks bind to 127.0.0.1 and ::1.","kind":"info","priority":"low","state":"complete","tags":["security"]}],"code_quality":[{"text":"Verified rather than assumed: caddy adapt shows that server listening on 127.0.0.1:80 and [::1]:80 instead of :80, and caddy validate still reports the rendered config valid.","kind":"info","priority":"low","state":"complete","tags":["testing"]}],"vulnerabilities":[{"text":"These blocks require no trusted certificate, so as written any machine on the LAN could send Host: name.localhost to port 80 and get a site root, with file_server browse to list it. Now unreachable off this machine.","kind":"info","priority":"low","state":"complete","tags":["security"]},{"text":"The pre-existing *.TLD HTTPS server still listens on :443 on every interface with the same file_server browse behind it. A certificate warning is the only thing in the way and a LAN client can ignore it. Binding it to loopback would close that but would also stop testing a site from a phone on the same network, which is a normal use of a local dev TLD. Filed as a concern rather than decided here.","kind":"task","priority":"low","state":"complete","resolved_by":"da5de4fc31b2cc3ebc50c67838020ec9d1f02e5d","tags":["security"],"history":[{"at":"2026-09-09T08:44:32.160Z","type":"resolved","by":"claude:84a94ca3-be6f-45bb-bfed-90e7f8bab9ad","commit":"da5de4fc31b2cc3ebc50c67838020ec9d1f02e5d"}]}],"code_style_suggestions":"None","reusability":"None","system_compatibility":"None"}
